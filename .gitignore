# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
.venv/
ENV/
env/

# Environment variables
.env

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Jupyter
.ipynb_checkpoints/

# =============================================================================
# Data Files - Exclude all real data, keep example files for format reference
# Example files use the pattern: filename.example.ext (e.g., model_ids.example.json)
# =============================================================================

# WhatsApp exports - raw chat data (keep example for format reference)
data/whatsapp/*.txt
!data/whatsapp/*.example.txt
data/whatsapp/zips/

# Training data - generated JSONL files (keep examples for format reference)
data/training/*.jsonl
!data/training/*.example.jsonl

# Conversations - saved chat logs (keep example for format reference)
data/conversations/*.json
!data/conversations/*.example.json

# Model tracking - contains real model IDs (keep example for format reference)
data/models/*.json
!data/models/*.example.json

# Plan files
*.plan.md
